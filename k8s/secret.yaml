apiVersion: v1
kind: Secret
metadata:
  name: lightrag-secret
  namespace: lightrag
type: Opaque
stringData:
  # 실제 배포 시 base64 인코딩된 값 또는 External Secrets 사용
  OPENAI_API_KEY: "your-openai-api-key-here"
